# Podfile
platform :ios, '13.0'
use_frameworks!

target 'apihub' do
  # 方案 1：锁定到稳定支持 iOS 的 4.9.0 版本（推荐，兼容性强）
#   pod 'WireCompiler', '4.9.0'
#   pod 'Wire', '4.9.0'

  # 方案 2：升级到最新版（需确认官方最新版支持 iOS，如 6.5.0）
#   pod 'WireCompiler', '~> 6.5.0'
#   pod 'Wire', '~> 6.5.0'

    pod 'WireCompiler'
    pod 'Wire'
end